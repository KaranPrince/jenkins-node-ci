{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "Sample Node.js app for Jenkins + Docker + ECR + EC2",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"(no tests yet)\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
